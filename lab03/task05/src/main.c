#define X 489 // задане тризначне число

int main() {

	int n1 = X / 100; // виокремлення першої цифри
	int n3 = X % 10; // виокремлення третьої цифри
	double y = (double)n1 / n3; // у скільки разів перша цифра більша, ніж третя
	
	double result = (double)((int)(y * 100)) / 100; // скорочення результату до другого знака після коми
	
	return 0;
}
