/**
 * @file lib.h
 * @brief Файл з прототипами функцій
 *
 * @author Vysotskyi O.
 * @date 17-jan-2021
 * @version 1.0
 */

#pragma once
#include <stdlib.h>
#include <string.h>

void reverseStr (char *str);
/**
 * Зміна порядку символів строки на протилежний
 *
 * Визначає розмір вхідної строки за допомогою функції strlen, створює за допомогою функції strdup копію строки, елементи якої записуються у зворотному порядку у вхідний масив
 */

